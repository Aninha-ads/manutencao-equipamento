import { GeralValidatorDirective } from './geral-validator.directive';

describe('GeralValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new GeralValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
